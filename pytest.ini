[pytest]
testpaths = agent
python_files = test_*.py
addopts = -m "not live_fiji and not benchmark"
markers =
    live_fiji: requires a separately started local Fiji/ImageJ instance
    benchmark: measures performance and is not a correctness test

